package com.example.testapplication;

public class Apple extends Mobile{

    Apple(String name, String processor, int storage, int ram) {
        this.setName(name);
        this.setProcessor(processor);
        this.setStorage(storage);
        this.setRam(ram);
    }
}